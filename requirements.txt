fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.9

# For PostgreSQL deployment (e.g. Render) only — not needed for local SQLite.
# Uncomment when deploying with a Postgres database:
# psycopg2-binary==2.9.10
